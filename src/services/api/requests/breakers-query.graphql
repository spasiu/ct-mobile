query Breakers {
  Users(where: { role: { _eq: BREAKER } }) {
    id
    first_name
    last_name
    image
    BreakerProfile {
      id
      twitter
      facebook
      instagram
      video
      bio
    }
  }
}
