query Breakers {
  Users(where: { role: { _eq: ADMIN } }) {
    id
    Profile {
      id
      first_name
      last_name
      twitter
      facebook
      instagram
      image
      video
      bio
    }
  }
}
