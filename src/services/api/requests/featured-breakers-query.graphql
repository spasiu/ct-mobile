query FeaturedBreakers {
  Users(where: { is_breaker: { _eq: true } }, limit: 6) {
    id
    first_name
    last_name
    image
    BreakerProfile {
      id
      twitter
      facebook
      instagram
      video
      bio
    }
  }
}
