query FeaturedBreakers {
  Users(where: { role: { _eq: ADMIN } }, limit: 6) {
    id
    first_name
    last_name
    image
    BreakerProfile {
      id
      twitter
      facebook
      instagram
      video
      bio
    }
  }
}
