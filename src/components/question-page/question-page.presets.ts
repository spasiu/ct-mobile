import { ViewStyle } from 'react-native';
import { styles as s } from 'react-native-style-tachyons';

export const containerWrapperStyle = [
  s.flx_i,
  s.pt4,
  s.aic,
  s.ph4,
  s.jcsb,
] as ViewStyle[];

export const optionsContainerStyle = [s.flx_i, s.w_100] as ViewStyle[];

export const actionContainerStyle = [s.w_100] as ViewStyle[];
